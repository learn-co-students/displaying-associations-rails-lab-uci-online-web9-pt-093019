<h1>All Artists</h1>

<% @artists.each do |artist| %>
    <h3><%= link_to artist.name, artist_path(artist) %> has <%= pluralize(artist.song_count, 'song') %></h3>
    <ul>
       <%  artist.songs.each do |song| %>
            <li><%= song.title %></li>
        <% end %>
    </ul>
<% end %>
